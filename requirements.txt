torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0
diffusers==0.36.0
transformers==4.55.2
accelerate
einops
easydict
numpy==1.26.4
tqdm
imageio[ffmpeg]
websockets
msgpack
opencv-python
matplotlib
ftfy
safetensors
Pillow

# Optional acceleration dependency:
# install separately with:
#   pip install flash-attn --no-build-isolation
# Keep it out of the default requirements flow because build isolation
# can fail before torch is visible in the build env.

# Optional post-training extras:
#   pip install lerobot==0.3.3 scipy wandb --no-deps
# Keep these out of the default runtime requirements because `lerobot==0.3.3`
# conflicts with the PyTorch 2.9 runtime used by this workspace unless it is
# installed as a no-deps add-on.
